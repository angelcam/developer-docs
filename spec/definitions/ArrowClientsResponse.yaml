type: object
title: A list of Arrow clients
properties:
  count:
    type: integer
    description: Number of all clients
  next:
    type:
      - string
      - 'null'
    format: uri
    description: Next page from pagination
  previous:
    type:
      - string
      - 'null'
    format: uri
    description: Previous page from pagination
  results:
    type: array
    description: A list of Arrow clients
    items:
      $ref: '#/definitions/ArrowClientListObject'
required:
  - count
  - next
  - previous
  - results
